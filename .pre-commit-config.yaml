default_stages:
  - pre-commit

repos:
  - repo: local
    hooks:
      - id: quality
        name: make quality
        entry: make quality-pre-commit
        language: system
        pass_filenames: false
        always_run: true
