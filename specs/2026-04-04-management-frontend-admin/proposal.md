# Management Frontend Admin

我在 `web` 目录放了一个简单的 prime vue 框架（没有内容）。你需要使用 `docs/management.yaml`，通过 `openapi-typescript` 来生成 SDK，然后做一个管理后台。首先需要一个登录页面，输入 token 进行登录。登录后需要有个导航栏，可以切换页面。然后根据 API 看需要做哪些页面，做个比较完备的后台计划出来。
