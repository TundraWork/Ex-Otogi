// Package eventcache provides an in-memory message cache module that records
// per-message event history, maintains projected message entities, exposes
// lookup services to other modules, and supports replying `~raw` and
// `~history` commands with cached projections.
package eventcache
