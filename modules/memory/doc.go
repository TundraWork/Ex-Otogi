// Package memory provides an in-memory memory module that records per-article
// event history, maintains projected article state, exposes lookup services to
// other modules, and handles `~raw` and `~history` command events with cached
// projections.
package memory
